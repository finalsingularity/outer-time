# Pick structure
function outer:generation/structures/forceload

# Place structure

execute at @s in outer:the_past run place template outer:empty ~ ~ ~
execute at @s in outer:the_present run place template outer:empty ~ ~ ~