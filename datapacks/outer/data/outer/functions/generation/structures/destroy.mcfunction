# Pick structure
function outer:generation/structures/forceload

# Place structure

execute at @s in outer:the_past run function outer:generation/structures/reset
execute at @s in outer:the_present run function outer:generation/structures/reset