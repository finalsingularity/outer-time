# Pick structure
function dng:generation/structures/forceload

# Place structure

execute at @s in dng:the_past run place template dng:empty ~ ~ ~
execute at @s in dng:the_present run place template dng:empty ~ ~ ~